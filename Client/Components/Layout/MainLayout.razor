@inherits LayoutComponentBase

@rendermode InteractiveServer

@* @if (!string.IsNullOrEmpty(ApplicationState?.ErrorMessage))
{
    <Alert Closable="true">
        @ApplicationState.ErrorMessage
    </Alert>
} *@

<div class="page">

    <AuthorizeView>
        <Authorized>
            <div class="sidebar">
                <NavMenu />
            </div>
        </Authorized>
    </AuthorizeView>

    <main>
        <Client.Components.Layout.Header />

        <article class="content px-4">
            @Body
        </article>
    </main>
</div>

<div id="blazor-error-ui" data-nosnippet>
    An unhandled error has occurred.
    <a href="." class="reload">Reload</a>
    <span class="dismiss">🗙</span>
</div>

@code {
    [CascadingParameter]
    public ApplicationState? ApplicationState { get; set; }

    // protected override void OnInitialized()
    // {
    //     if (ApplicationState != null)
    //     {
    //         ApplicationState.PropertyChanged += (sender, args) =>
    //         {
    //             if (args.PropertyName == nameof(ApplicationState.ErrorMessage))
    //             {
    //                 StateHasChanged();
    //             }
    //         };
    //     }
    // }
}